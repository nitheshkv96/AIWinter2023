***Computational Learning***
***Project - 02***
*** Neural Networks for Hand Written DIgits Classification***

Author	: Nithesh Veerappa
Date		: 04/18/2023

*Deliverables*
1. nnetDigitClassifier.py (Contains Source code defining the 'NNet' Class for the given hand written digits classification)
2. readme.txt  (Contains instruction to run the code)
4. nnetDigitClassifier_Report.pdf (Describing the problem, source code and conclusion with results)

*Librabries Used*
1. numpy (For math and array creation)
2. matplotlib (For plotting results)

*Instruction*
After placing nnetDigitClassifier.py, optdigits_test.dat, optdigits_train.dat and optdigits_trial.dat in the same folder, follow below steps in the same sequence:
1. Installation of all the special libraries used in the source code if not installed already
	$ !pip install matplotlib
	$ !pip install numpy
2. Two ways to run the script
   a. Calling the script from command prompt:
	>> python nnetDigitClassifier.py
   b. Running the script using an editor (VScode, spyder, vim etc)
	Run the script by clicking run 
4. After following the above steps, results will be printed in the command window with 3 pop up plots.

Hand Written Digits Classification is Complete !!
